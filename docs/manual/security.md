# Security


## Authentication

All requests must present a bearer token. Tokens are signed with a key from the active keyring and carry a tenant claim which scopes every subsequent operation. All requests must present a bearer token. Tokens are signed with a key from the active keyring and carry a tenant claim which scopes every subsequent operation. All requests must present a bearer token. Tokens are signed with a key from the active keyring and carry a tenant claim which scopes every subsequent operation. All requests must present a bearer token. Tokens are signed with a key from the active keyring and carry a tenant claim which scopes every subsequent operation. All requests must present a bearer token. Tokens are signed with a key from the active keyring and carry a tenant claim which scopes every subsequent operation. All requests must present a bearer token. Tokens are signed with a key from the active keyring and carry a tenant claim which scopes every subsequent operation.


## Multi-tenancy

Tenant isolation is enforced at the metadata layer. Every query is rewritten to include a tenant predicate before it reaches the storage engine. Application code must never rely on filtering results after retrieval, as this places correctness in the wrong layer. Tenant isolation is enforced at the metadata layer. Every query is rewritten to include a tenant predicate before it reaches the storage engine. Application code must never rely on filtering results after retrieval, as this places correctness in the wrong layer. Tenant isolation is enforced at the metadata layer. Every query is rewritten to include a tenant predicate before it reaches the storage engine. Application code must never rely on filtering results after retrieval, as this places correctness in the wrong layer. Tenant isolation is enforced at the metadata layer. Every query is rewritten to include a tenant predicate before it reaches the storage engine. Application code must never rely on filtering results after retrieval, as this places correctness in the wrong layer. Tenant isolation is enforced at the metadata layer. Every query is rewritten to include a tenant predicate before it reaches the storage engine. Application code must never rely on filtering results after retrieval, as this places correctness in the wrong layer. Tenant isolation is enforced at the metadata layer. Every query is rewritten to include a tenant predicate before it reaches the storage engine. Application code must never rely on filtering results after retrieval, as this places correctness in the wrong layer.
